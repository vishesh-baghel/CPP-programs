#include<bits/stdc++.h>
using namespace std;

vector<int> spirallyTraverse(vector<int> matrix, int r, int c) {
  int total = r*c;
  int startingRow = 0;
  int endingRow = r - 1;
  int startingCol = 0;
  int endingCol = c - 1;

  vector<int> sp;
  int count = 0;
  while (count = startingCol && count = startingRow) 
}

int main() {

  return 0;
}